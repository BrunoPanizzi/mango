import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function TaskEditableItem() {
  // TODO: acabar o componente, adicionar modos questão de alternativa,
  // questão de texto e modo upload de imagem
  return (
    <View>
      <Text>item</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
